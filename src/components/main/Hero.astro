---
import Image from 'astro/components/Image.astro';
import AnimateOnScroll from '../../animations/AnimateOnScroll.astro';
import heroImg from '../../assets/hero-img.webp';
---

<section class='hero padding-horizontal padding-bottom'>
  <div class='hero-container'>
    <AnimateOnScroll animation='up-slide' duration={0.5}>
      <div class='hero-content'>
        <h1>Šlep služba Surčin i Beograd - pomoć na putu 0-24</h1>
        <h2 class='hero-description'>
          Bilo da je u pitanju kvar, bušna guma ili havarija, šlep služba Kića je tu za vas! Nalazimo se u Surčinu tik
          uz <strong>Autoput E-75</strong>
          što nam omogućava da budemo na vašoj adresi u rekordnom roku na sledećim lokacijama: <strong
            >Surčin i okolina:</strong
          > Dobanovci, Jakovo, Boljevci, Bečmen, Progar. <strong>Novi Beograd i Zemun: Aerodrom Nikola Tesla</strong>,
          Ledine, Bežanijska kosa.
        </h2>
        <div class='hero-ctas'>
          <a href='#request-tow' class='cta-primary'>
            <span>Zatražite šlep odmah</span>
            <span class='cta-icon'>
              <img src='/img/icons/right-arrow.svg' alt='' width='20' height='20' />
            </span>
          </a>
          <!-- <a href='tel:+14805550103' class='cta-phone'>+(480) 555-0103</a> -->
        </div>
      </div>
    </AnimateOnScroll>

    <div class='hero-image-wrapper'>
      <Image src={heroImg} alt='Roadside assistance tow truck' class='hero-image' width='1200' height='800' />
    </div>

    <div class='steps-container observer-container'>
      <div class='step-badge' data-step='1'>
        <span class='step-number'>Korak 1</span>
        <span class='step-text'>Pozovite šlep službu</span>
      </div>
      <div class='step-badge' data-step='2'>
        <span class='step-number'>Korak 2</span>
        <span class='step-text'>Brz odgovor i polazak</span>
      </div>
      <div class='step-badge' data-step='3'>
        <span class='step-number'>Korak 3</span>
        <span class='step-text'>Bezbedan transport vozila</span>
      </div>
      <div class='step-badge' data-step='4'>
        <span class='step-number'>Korak 4</span>
        <span class='step-text'>Isporuka gde vam odgovara</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: var(--space-800);
  }
  .hero-container {
    width: 100%;
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-700);
  }

  .hero-content {
    max-width: 60%;
  }

  h1 {
    color: var(--text-header);
    margin-bottom: var(--space-400);
  }

  h2 {
    color: var(--text-body);
    font-size: var(--fs-400) !important;
    font-weight: 400;
    line-height: 1.6;
    margin-bottom: var(--space-600);
  }

  .hero-ctas {
    display: flex;
    align-items: center;
    gap: var(--space-400);
    flex-wrap: wrap;
  }

  .cta-primary {
    display: inline-flex;
    align-items: center;
    gap: var(--space-300);
    background: #000000;
    color: #ffffff;
    text-decoration: none;
    padding: 12px 12px 12px 24px;
    border-radius: var(--rounded-full);
    font-size: var(--fs-300);
    transition: all 0.4s cubic-bezier(0.26, 0.51, 0.58, 1);
  }

  .cta-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: var(--primary);
    border-radius: 50%;
    flex-shrink: 0;
    transition: background-color 0.4s cubic-bezier(0.26, 0.51, 0.58, 1);
  }

  .cta-icon img {
    display: block;
    transition:
      transform 0.4s ease,
      filter 0.4s ease;
  }

  .cta-primary:hover {
    background: var(--primary);
    color: black;
  }

  .cta-primary:hover .cta-icon {
    background: black;
  }

  .cta-primary:hover .cta-icon img {
    filter: invert(1);
    transform: rotate(-45deg);
  }

  .cta-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: var(--primary);
    border-radius: 50%;
    flex-shrink: 0;
  }

  .cta-icon img {
    display: block;
  }

  .cta-phone {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: var(--text-header);
    text-decoration: none;
    padding: var(--space-400) var(--space-600);
    border: 2px solid var(--text-header);
    border-radius: var(--rounded-full);
    font-weight: 600;
    font-size: var(--fs-300);
    transition: all 0.2s cubic-bezier(0.26, 0.51, 0.58, 1);
  }

  .cta-phone:hover {
    background: var(--text-header);
    color: var(--bg);
  }

  .hero-image-wrapper {
    width: 100%;
    opacity: 0;
    animation: fadeIn 0.8s cubic-bezier(0.26, 0.51, 0.58, 1) 0.4s forwards;
  }

  .hero-image {
    width: 100%;
    height: auto;
    display: block;
    border-radius: var(--rounded-lg);
    object-fit: cover;
  }

  .steps-container {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    margin-top: -2rem;
  }

  .step-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-300);
    color: #000000;
    border-radius: var(--rounded-full);
    opacity: 0;
    transform: translateX(-20px);
    transition:
      opacity 0.3s cubic-bezier(0.26, 0.51, 0.58, 1),
      transform 0.3s cubic-bezier(0.26, 0.51, 0.58, 1);
  }

  .step-number {
    font-weight: 500;
    font-size: var(--fs-200);
    background: #000;
    color: #fff;
    padding: var(--space-100) var(--space-300);
    border-radius: var(--rounded-full);
  }

  .step-text {
    font-weight: 500;
    font-size: var(--fs-200);
  }

  /* Stagger animation for step badges when visible */
  .observer-container.is-visible .step-badge[data-step='1'] {
    transition-delay: 0s;
    opacity: 1;
    transform: translateX(0);
  }

  .observer-container.is-visible .step-badge[data-step='2'] {
    transition-delay: 0.1s;
    opacity: 1;
    transform: translateX(0);
  }

  .observer-container.is-visible .step-badge[data-step='3'] {
    transition-delay: 0.2s;
    opacity: 1;
    transform: translateX(0);
  }

  .observer-container.is-visible .step-badge[data-step='4'] {
    transition-delay: 0.3s;
    opacity: 1;
    transform: translateX(0);
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }

  @media (max-width: 768px) {
    .hero-content {
      max-width: 100%;
    }

    .hero-ctas {
      gap: var(--space-300);
    }

    .cta-primary {
      font-size: var(--fs-200);
    }

    .cta-icon {
      width: 40px;
      height: 40px;
    }

    .cta-icon img {
      width: 16px;
      height: 16px;
    }

    .cta-phone {
      padding: var(--space-300) var(--space-500);
      font-size: var(--fs-200);
    }

    .steps-container {
      flex-direction: column;
      gap: var(--space-300);
      margin-top: -1rem;
    }

    .step-badge {
      transform: translateY(-20px);
    }

    /* Mobile stagger - top to bottom */
    .observer-container.is-visible .step-badge[data-step='1'] {
      transition-delay: 0.4s;
      opacity: 1;
      transform: translateY(0);
    }

    .observer-container.is-visible .step-badge[data-step='2'] {
      transition-delay: 0.5s;
      opacity: 1;
      transform: translateY(0);
    }

    .observer-container.is-visible .step-badge[data-step='3'] {
      transition-delay: 0.6s;
      opacity: 1;
      transform: translateY(0);
    }

    .observer-container.is-visible .step-badge[data-step='4'] {
      transition-delay: 0.7s;
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const stepsContainer = document.querySelector('.steps-container.observer-container');

    if (!stepsContainer) return;

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            observer.unobserve(entry.target);
          }
        });
      },
      {
        rootMargin: '0px 0px -100px 0px',
        threshold: 0.2,
      },
    );

    observer.observe(stepsContainer);
  });
</script>
